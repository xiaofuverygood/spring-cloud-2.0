server:
  port: 80
spring:
  application:
    name: cloud-consumer-order
  cloud:
    # ----------------------- 注册中心 ----------------------------------------------
    consul:
      host: localhost
      port: 8500
      discovery:
        prefer-ip-address: true #优先使用服务ip进行注册
        service-name: ${spring.application.name} #配置当前服务注册到里面使用的名字
    # ------------------------------------------------------------------------------